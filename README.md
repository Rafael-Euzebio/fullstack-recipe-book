# Instructions

### Frontend
```
cd frontend
npm install
npm run dev
```

### Backend
By default Next runs on port 3000, same as Nest. `.env` sets Nest port to 8080

```
cd backend
npm install
npm run start:dev
```
